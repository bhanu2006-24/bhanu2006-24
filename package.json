{
  "name": "profile-metrics-generator",
  "version": "1.0.0",
  "description": "Generates self-hosted GitHub metrics for the profile README",
  "main": "scripts/generate_metrics.js",
  "scripts": {
    "generate": "node scripts/generate_metrics.js"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "dotenv": "^16.3.1"
  }
}
